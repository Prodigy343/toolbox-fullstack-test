services:
  api:
    build: ./api
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    environment:
      - DEV_SERVER_HOST=0.0.0.0
      - API_PROXY_TARGET=http://api:3000
    depends_on:
      - api
